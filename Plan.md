# 家教资源整合平台项目文档（信息撮合版）


## 一、功能需求

### 1.1 核心功能模块

#### 模块一：用户管理

| 功能 | 描述 |
|------|------|
| 注册与登录 | 教师自助注册并绑定微信、QQ或手机号；手机号使用验证码，管理员身份由系统初始化创建 |
| 角色权限 | 三种角色：**最高管理员**（全权限）、**普通管理员**（接收简历+发布需求）、**教师**（浏览需求、申请接单、课程管理） |
| 教师资料 | 教师注册后完善个人资料（姓名、性别、学历、毕业院校、擅长科目、教学经验、可授课时间与区域、个人简介、985/211/重本标签等） |
| 简历投递 | 教师通过平台上传个人简历文档（PDF/Word）或在线填写详细简历表单，提交后由普通管理员接收 |

#### 模块二：需求管理（管理员专属）

**需求发布字段说明：**

| 字段 | 必填 | 类型 | 说明 |
|------|:----:|------|------|
| 地址 | ✅ | 定位 | 支持地图选点定位（高德/腾讯地图API），记录详细地址+经纬度 |
| 科目 | ✅ | 标签选择 | 下拉选择，如：语文、数学、英语、物理、化学、生物、历史、地理、政治、奥数、编程、钢琴、美术等 |
| 年级 | ✅ | 标签选择 | 下拉选择，如：小学一年级～六年级、初一～初三、高一～高三 |
| 性别 | ❌ | 单选 | 教师性别要求：男/女/不限 |
| 基本成绩 | ❌ | 文本/标签 | 学员当前成绩描述，如“70/100分”“班级中游”，用于教师评估匹配度 |
| 薪酬范围 | ❌ | 文本 | 如“200-300元/小时”或“面议”，仅作参考展示 |
| 备注 | ❌ | 文本域 | 补充说明，如上课时间要求、教材版本、特殊需求等 |
| 资质要求 | ❌ | 标签多选 | 如：985/211、重本、师范类、有竞赛经验、有教师资格证等 |
| 985/211/重本 | ❌ | 独立标签 | 非必选标签，快速标记教师学历门槛（三个可独立勾选或组合） |

**发布权限：**

| 操作 | 普通管理员 | 最高管理员 |
|------|:--------:|:--------:|
| 发布需求 | ✅ | ✅ |
| 需求列表 | ✅ 查看所有 | ✅ 查看所有 |
| 需求编辑/关闭 | ✅ 可编辑/关闭未分配的需求 | ✅ 可编辑/关闭任意需求 |
| 需求分配（匹配教师） | ❌ | ✅ |
| 查看教师简历 | ✅ 仅可查看和下载 | ✅ 可查看和下载 |
| 教师审核（启用/禁用） | ❌ | ✅ |

#### 模块三：教师匹配与接单

| 功能 | 描述 |
|------|------|
| 需求广场 | 教师端展示所有“待接单”需求，支持多维度**筛选**与**排序** |
| 筛选功能 | 按以下条件组合筛选：科目、年级、区域（基于定位距离/行政区）、薪酬范围、性别要求、资质标签（985/211/重本/师范类等）、成绩要求 |
| 排序功能 | 支持以下排序方式：发布时间（默认，最新优先）、距离最近、薪酬高低、匹配度（基于教师资料与需求字段的契合度） |
| 申请接单 | 教师点击进入需求详情后，附上简要自荐说明提交申请 |
| 简历投递入口 | 教师可在个人中心独立投递/更新简历，普通管理员收到简历通知后可在后台查看 |
| 匹配通知 | 教师收到最高管理员的匹配结果通知（通过/未通过），匹配成功后生成课程订单 |
| 联系方式互推 | 匹配成功后，平台同时向家长和教师推送对方的联系方式（电话/微信），后续沟通与费用结算由双方自行对接 |

**筛选条件面板：**

| 筛选项 | 控件类型 | 说明 |
|------|------|------|
| 科目 | 下拉多选 | 语文/数学/英语/物理/化学/生物/历史/地理/政治/奥数/编程/钢琴/美术等 |
| 年级 | 下拉多选 | 小学一年级～六年级/初一～初三/高一～高三 |
| 区域 | 地图选点/行政区下拉 | 基于定位显示附近需求，或按城市-区-街道筛选 |
| 薪酬范围 | 区间滑块 | 如 100-200元/小时，200-300元/小时等 |
| 性别要求 | 单选 | 男/女/不限 |
| 资质标签 | 多选 | 985/211/重本/师范类/有竞赛经验/有教师资格证等 |
| 基本成绩 | 下拉选项 | 优秀/良好/中等/待提升/不限 |

**排序方式：**

| 排序项 | 说明 |
|------|------|
| 最新发布（默认） | 按需求发布时间倒序排列 |
| 距离最近 | 基于教师当前位置（或默认区域）计算需求地址距离，由近到远 |
| 薪酬最高 | 按薪酬范围从高到低排序 |
| 薪酬最低 | 按薪酬范围从低到高排序 |
| 匹配度最高 | 系统根据教师资料与需求字段综合计算匹配分数 |

**需求列表展示卡片内容：**

| 展示项 | 说明 |
|------|------|
| 科目 + 年级 | 醒目展示，如“高一数学” |
| 地址 | 显示简要地址 + 距离（如“3.2km”） |
| 薪酬范围 | 展示如“200-300元/小时” |
| 性别要求 | 展示“男教师/女教师/不限” |
| 标签徽章 | 显示需求所带的资质标签（985/211/重本等） |
| 发布时间 | 如“2小时前发布” |
| 申请按钮 | 点击进入需求详情并提交申请 |

**筛选排序逻辑补充说明：**

| 场景 | 处理方式 |
|------|---------|
| 未开启定位 | 隐藏“距离最近”排序选项，区域筛选默认展示全部 |
| 无匹配结果 | 显示“暂无匹配需求，建议调整筛选条件”提示 |
| 多条件组合筛选 | 各筛选条件为AND关系，同时生效 |
| 匹配度计算 | 权重分配：科目匹配40% + 年级匹配25% + 标签匹配20% + 距离15%，得分高者优先 |
| 排序与筛选先后 | 先应用筛选条件过滤数据，再对结果集执行排序 |
| 默认状态 | 不应用任何筛选，按“最新发布”排序，展示全部待接单需求 |
| 缓存策略 | 筛选条件变更时优先从Redis缓存读取，缓存过期时间30秒 |

#### 模块四：课程管理

| 功能 | 描述 |
|------|------|
| 我的课程 | 教师查看已确认的课程订单列表及详情（含家长联系方式） |
| 课程提醒 | 定时推送即将开始的课程通知 |
| 教学管理 | 教师创建教学计划，每节课后记录授课内容和学员表现 |
| 进度追踪 | 系统记录累计授课课时、学员知识掌握反馈，生成阶段性学习报告 |

#### 模块五：评价与反馈

| 功能 | 描述 |
|------|------|
| 课后评价 | 管理员回访家长后代为录入评价（评分1-5分和文字反馈） |
| 评分展示 | 教师综合评分根据家长评价动态计算，在教师个人主页公开展示 |
| 投诉与仲裁 | 最高管理员可记录投诉工单，跟进处理进度（平台仅作沟通协调） |

### 1.2 前台与后台功能

- **教师前台**：需求广场（含筛选排序）、我的课程、教学记录、个人资料、简历投递
- **普通管理员后台**：需求发布与管理、教师简历接收与查看
- **最高管理员后台**：全部管理功能（需求管理、教师审核与禁用、课程订单管理、评价管理、系统配置、普通管理员账号管理）

### 1.3 非功能性需求

| 指标 | 要求 |
|------|------|
| 页面响应时间 | 90%操作应在2秒内完成 |
| 搜索响应 | ≤1.5秒（1000条需求数据内） |
| 消息通知延迟 | ≤500ms |
| 日常并发 | ≥3000并发用户（教师端） |
| 高峰并发 | ≥6000并发用户（寒暑假、周末） |
| 数据库查询 | 需求列表查询≤1秒（10万条数据） |
| 地图定位精度 | 定位误差≤50米 |
| 身份可信 | 写操作操作者必须来自服务端登录会话，禁止信任客户端提交的用户ID |
| 接口安全 | 限制请求大小与来源，统一安全响应头，对登录、验证码和敏感数据实施防刷与脱敏 |
| 发布可追溯 | 每次正式发布具备版本号、自动测试、构建校验、回滚方案和发布记录 |


## 二、技术架构

### 2.1 技术选型

| 层级 | 技术 | 说明 |
|------|------|------|
| 后端框架 | Spring Boot | “约定优于配置”，快速集成各组件 |
| 前端框架 | Vue.js | 响应式界面，适配PC与移动端 |
| UI组件库 | Element UI | 友好的交互界面 |
| 数据库 | MySQL 8.0 | 支持GIS地理位置查询、JSON字段 |
| 地图服务 | 高德地图API / 腾讯地图API | 地址定位与逆地理编码 |
| ORM框架 | MyBatis-Plus | 优化数据库交互 |
| 权限控制 | Spring Security | 三角色权限管控 |
| 缓存 | Redis | 缓存热门需求列表与筛选结果 |
| 对象存储 | 阿里云OSS | 存储教师简历文档 |
| 接口风格 | RESTful API | 前后端分离，保障可扩展性 |

### 2.2 系统架构

```

┌─────────────────────────────────────────────────┐
│                   前端（Vue.js）                  │
│     教师端  │ 普通管理后台  │ 最高管理后台        │
└─────────────────────┬───────────────────────────┘
│ RESTful API
┌─────────────────────▼───────────────────────────┐
│              Spring Boot 后端服务                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │用户管理  │ │需求管理  │ │匹配引擎  │        │
│  ├──────────┤ ├──────────┤ ├──────────┤        │
│  │课程管理  │ │评价系统  │ │消息通知  │        │
│  └──────────┘ └──────────┘ └──────────┘        │
└─────────────────────┬───────────────────────────┘
│
┌─────────────────────▼───────────────────────────┐
│              数据层（MySQL + Redis）              │
└─────────────────────────────────────────────────┘

```


## 三、数据库设计

### 3.1 核心数据表

#### 用户表（user）

| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | BIGINT | 用户ID（主键） |
| display_name | VARCHAR(50) | 用户展示名称 |
| login_method | VARCHAR(16) | 登录方式（WECHAT/QQ/PHONE） |
| login_id | VARCHAR(100) | 第三方身份标识，与登录方式组成唯一键 |
| role_type | TINYINT | 角色类型（1-最高管理员，2-普通管理员，3-教师） |
| phone_number | VARCHAR(20) | 手机号（唯一） |
| email | VARCHAR(50) | 邮箱（可选） |
| is_enabled | BOOLEAN | 账号是否启用 |
| register_time | DATETIME | 注册时间 |
| last_login | DATETIME | 最后登录时间 |

#### 教师资料表（teacher_profile）

| 字段 | 类型 | 说明 |
|------|------|------|
| teacher_id | BIGINT | 教师ID（主键，关联user） |
| real_name | VARCHAR(50) | 真实姓名 |
| gender | TINYINT | 性别（1-男，2-女） |
| education | VARCHAR(50) | 学历（本科/硕士/博士等） |
| graduate_school | VARCHAR(100) | 毕业院校 |
| is_985 | BOOLEAN | 是否985院校毕业 |
| is_211 | BOOLEAN | 是否211院校毕业 |
| is_key_university | BOOLEAN | 是否重本（双非重点本科） |
| subjects | JSON | 擅长科目（如["数学","物理"]） |
| teaching_experience | TEXT | 教学经验描述 |
| expected_rate | VARCHAR(50) | 期望课时费（仅展示） |
| available_time | JSON | 可授课时间段 |
| service_area | JSON | 可服务区域 |
| personal_intro | TEXT | 个人简介 |
| avg_rating | DECIMAL(3,2) | 平均评分 |
| contact_phone | VARCHAR(20) | 联系电话（匹配成功后向家长展示） |
| contact_wechat | VARCHAR(50) | 微信号（匹配成功后向家长展示） |
| has_teacher_cert | BOOLEAN | 是否有教师资格证 |

#### 教师简历表（teacher_resume）

| 字段 | 类型 | 说明 |
|------|------|------|
| resume_id | BIGINT | 简历ID（主键） |
| teacher_id | BIGINT | 教师ID（外键） |
| resume_file_url | VARCHAR(500) | 简历文档存储路径 |
| resume_json | JSON | 在线填写简历的结构化数据 |
| is_active | BOOLEAN | 是否为当前生效简历 |
| submit_time | DATETIME | 投递/更新时间 |
| status | TINYINT | 状态（0-待查看，1-已查看，2-已筛选） |

#### 需求表（demand）

| 字段 | 类型 | 说明 |
|------|------|------|
| demand_id | BIGINT | 需求ID（主键） |
| admin_id | BIGINT | 发布管理员ID（外键） |
| parent_name | VARCHAR(50) | 家长姓名 |
| parent_phone | VARCHAR(20) | 家长联系电话 |
| parent_wechat | VARCHAR(50) | 家长微信号（可选） |
| address | VARCHAR(200) | 详细地址 |
| longitude | DECIMAL(10,7) | 经度（地图定位） |
| latitude | DECIMAL(10,7) | 纬度（地图定位） |
| subject | VARCHAR(20) | 科目（必填，下拉选择） |
| grade | VARCHAR(20) | 年级（必填，下拉选择） |
| teacher_gender | TINYINT | 教师性别要求（1-男，2-女，3-不限） |
| basic_score | VARCHAR(50) | 基本成绩（如“80/100分”“班级前10”） |
| salary_range | VARCHAR(50) | 薪酬范围（如“200-300元/小时”） |
| remark | TEXT | 备注（上课时间、教材版本、特殊需求等） |
| qualification_tags | JSON | 资质要求标签（如["师范类","有竞赛经验"]） |
| is_985_required | BOOLEAN | 是否要求985 |
| is_211_required | BOOLEAN | 是否要求211 |
| is_key_university_required | BOOLEAN | 是否要求重本 |
| status | TINYINT | 状态（0-待接单，1-匹配中，2-授课中，3-已完成，4-已关闭） |
| create_time | DATETIME | 发布时间 |
| close_time | DATETIME | 关闭时间 |

#### 需求申请表（demand_application）

| 字段 | 类型 | 说明 |
|------|------|------|
| application_id | BIGINT | 申请ID（主键） |
| demand_id | BIGINT | 关联需求ID（外键） |
| teacher_id | BIGINT | 申请教师ID（外键） |
| self_intro | TEXT | 教师自荐说明 |
| apply_time | DATETIME | 申请时间 |
| status | TINYINT | 状态（0-待审核，1-通过，2-未通过） |
| confirm_time | DATETIME | 审核确认时间 |
| confirm_admin_id | BIGINT | 确认的最高管理员ID |

#### 课程订单表（course_order）

| 字段 | 类型 | 说明 |
|------|------|------|
| order_id | BIGINT | 订单ID（主键） |
| demand_id | BIGINT | 关联需求ID（外键） |
| teacher_id | BIGINT | 确认接单的教师ID（外键） |
| admin_id | BIGINT | 确认订单的最高管理员ID（外键） |
| order_status | TINYINT | 状态（1-待授课，2-授课中，3-已完成，4-已取消） |
| create_time | DATETIME | 订单创建时间 |
| finish_time | DATETIME | 完成时间 |
| contact_shared | BOOLEAN | 双方联系方式是否已互推 |

#### 授课记录表（teaching_record）

| 字段 | 类型 | 说明 |
|------|------|------|
| record_id | BIGINT | 记录ID（主键） |
| order_id | BIGINT | 关联订单ID（外键） |
| lesson_date | DATE | 上课日期 |
| lesson_duration | DECIMAL(4,2) | 本次课时（小时） |
| content | TEXT | 授课内容 |
| student_performance | TEXT | 学员课堂表现 |
| teacher_notes | TEXT | 教师内部备注 |

#### 评价表（feedback）

| 字段 | 类型 | 说明 |
|------|------|------|
| feedback_id | BIGINT | 评价ID（主键） |
| order_id | BIGINT | 关联订单ID（外键） |
| rating_score | TINYINT | 评分（1-5分） |
| comment_text | TEXT | 评价内容 |
| feedback_source | TINYINT | 来源（1-家长，2-教师自评） |
| submit_time | DATETIME | 提交时间 |
| submit_admin_id | BIGINT | 录入评价的管理员ID |

### 3.2 索引策略

- 对 `demand` 表的 `subject`、`grade`、`status`、`create_time` 建立联合索引
- 对 `demand` 的经纬度字段建立**空间索引**，支持按距离排序与筛选
- 对 `demand_application` 的 `demand_id` 和 `teacher_id` 建立唯一联合索引
- 对 `teacher_resume` 的 `teacher_id` 和 `is_active` 建立联合索引
- 对 `teacher_profile` 的 `is_985`、`is_211`、`is_key_university` 建立普通索引，加速筛选


## 四、运营与盈利模式

### 4.1 盈利模式

| 模式 | 说明 |
|------|------|
| 信息撮合费 | 匹配成功后，向家长收取一次性信息费（如50-200元） |
| 教师入驻费 | 教师注册后可免费浏览需求，如需主动申请接单，可收取小额年费/月费 |
| 增值服务 | 教师付费置顶展示、优先推荐等推广服务 |
| 广告收入 | 平台流量变现（如教育机构广告位） |

> 注：所有费用均在线下或通过第三方支付完成，平台自身不搭建支付结算系统。

### 4.2 运营策略

- **信息真实性核验**：管理员发布需求前须电话确认家长信息真实有效
- **标签匹配**：利用985/211/重本标签实现教师快速筛选
- **简历筛选**：普通管理员接收简历后初步筛选，标记推荐给最高管理员
- **质量回访**：匹配成功后，管理员定期回访家长，了解教学满意度

### 4.3 合规提醒

- 平台仅作为信息撮合方，不参与教学服务交付和费用结算
- 教师与家长自行签订服务协议，平台提供标准协议模板参考
- 平台不承担因教学服务质量引发的经济纠纷责任


## 五、项目计划

### 5.1 开发阶段

| 阶段 | 周期 | 主要工作 |
|------|------|---------|
| 需求分析 | 2周 | 业务流程梳理、原型设计、角色权限确认 |
| 数据库设计 | 1周 | 表结构设计、索引优化、GIS字段配置 |
| 后端开发 | 3周 | 用户权限、需求管理（含地图定位）、简历投递、匹配引擎、课程管理 |
| 前端开发 | 3.5周 | 教师端（含筛选排序组件）+ 普通管理后台 + 最高管理后台（含地图组件集成） |
| 测试与部署 | 2周 | 功能测试、权限隔离测试、压力测试、上线部署 |

### 5.2 优化方向

- **短期**：需求与教师匹配引入标签化推荐（科目、年级、985/211标签、区域距离）
- **中期**：上线教师端小程序，支持地图查看附近需求
- **长期**：接入智能匹配算法，自动推荐匹配度最高的教师给最高管理员


## 六、附录

### 6.1 核心业务流程（简图）

```

【简历流】
教师注册 → 完善资料（含985/211/重本标签）→ 投递简历 → 普通管理员接收查看 → 最高管理员审核启用

【需求流】
家长线下联系 → 管理员发布需求（地址定位、科目、年级、性别、成绩、薪酬、备注、资质标签）
→ 教师端需求广场展示（支持筛选排序）→ 教师按科目/年级/标签筛选并申请 → 最高管理员撮合确认
→ 生成课程订单 → 双方互推联系方式 → 自行对接授课 → 平台回访录入评价

```

### 6.2 权限职责对照表

| 操作 | 教师 | 普通管理员 | 最高管理员 |
|------|:----:|:--------:|:--------:|
| 注册/登录 | ✅ | ✅ | ✅ |
| 投递/更新简历 | ✅ | ❌ | ❌ |
| 浏览需求广场（含筛选排序） | ✅ | ❌ | ❌ |
| 申请接单 | ✅ | ❌ | ❌ |
| 查看教师简历 | ❌ | ✅（仅查看） | ✅ |
| 发布需求 | ❌ | ✅ | ✅ |
| 编辑/关闭需求 | ❌ | ✅（仅未分配） | ✅ |
| 教师账号启用审核 | ❌ | ❌ | ✅ |
| 需求匹配 | ❌ | ❌ | ✅ |
| 互推联系方式 | ❌ | ❌ | ✅ |
| 系统配置 | ❌ | ❌ | ✅ |
| 普通管理员账号管理 | ❌ | ❌ | ✅ |

### 6.3 技术参考资源

- Spring Boot 官方文档
- Vue.js + Element UI 开发指南
- 高德地图JS API / 腾讯地图位置服务
- MySQL 8.0 空间索引文档
- Spring Security 权限精细化配置

### 6.4 平台免责声明

> 本平台仅作为家教供需信息展示与撮合平台，不参与任何教学服务交付和费用结算。教师与家长之间的一切教学安排、课时费用、服务标准等，均由双方自行协商。平台对教学服务质量、费用纠纷等不承担任何法律责任。
